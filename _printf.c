#include "main.h"
/**
 * _printf - formats and prints data
 *
 * @format: string to print
 *
 * @...: arguements to the function, ie., formats
 *
 * Return: On success, number of characters printed excluding the
 * null byte used to end output to string
 */
int _printf(const char * const format, ...)
{
	va_list args;

	va_start(args, format);


	return (0);
}
